package pack;

public class BoletinMoodle9 {

	/*public static void main(String[] args) {
		// TODO Auto-generated method stub
		int vector[] = new int[10];
		vector[10] = (int) (Math.random() * 100);
		System.out.println(vector);
		int numero = vector[10];
		int pos = busqSecuencial(numero, vector);
		numero = vector[10];
		pos = busqDicotomica(numero, vector);

	}

	public static int busqSecuencial(int n, int[] vector) {
		int posicion = -1;
		for (int i = 0; i < vector.length; i++) {
			if (vector[i] == n) {
				posicion = i;
				break;
			}
		}
		return posicion;
	}

	public static void ordenacion(int[] vector) {
		for (int i = 0; i < vector.length; i++) {
			for (int j = 0; j < vector.length - 1; j++) {
				if (vector[j] > vector[j + 1]) {
					int temp = vector[j];
					vector[j] = vector[j + 1];
					vector[j + 1] = temp;
				}
			}
		}

	}

	public static int busqDicotomica(int n, int[] vector) {
		for (int i = 0; i < vector.length; i++)
			System.out.println("vector[" + i + "]: " + vector[i]);

		int resultado= ordenacion(n, vector);

		if (resultado != -1) {
			System.out.println(resultado);
		} else {
			System.out.println("No esta ordenado");
		}

	}
*/
}
